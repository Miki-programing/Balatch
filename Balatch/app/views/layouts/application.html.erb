<!DOCTYPE html>
<html>
  <head>
    <title>Balatch</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%#= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-dark">
        <div class="container">
          <!--<a class= "navbar-brand p-3" href="/">Balatch</a>-->
          <%= link_to "Balatch", root_path, class:"text-light" %>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <li>
                <%= link_to histories_path, class:"nav-link" do %>
                  <i class="fas fa-calendar-alt text-light"></i>
                <% end %>
              </li>
              <li>
                <%= link_to methods_path, class:"nav-link" do %>
                  <i class="fas fa-th-large text-light"></i>
                <% end %>
              </li>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main>
      <div class="container">
        <%= yield %>
      </div><!-- /.container -->
    </main>
  </body>
</html>
